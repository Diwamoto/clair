interface:
  display_name: "Clair Session Commit"
  short_description: "Partition, verify, and commit Clair work safely"
  default_prompt: "Use $clair-session-commit to inspect the current Clair changes, partition and verify the finished outcomes, commit only safe slices, and push only if I explicitly request it."

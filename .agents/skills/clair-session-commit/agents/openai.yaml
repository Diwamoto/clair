interface:
  display_name: "Clair Session Commit"
  short_description: "Split, verify, commit, and push Clair work safely"
  default_prompt: "Use $clair-session-commit to inspect the current Clair changes, split them into coherent commits, verify each slice, commit them, and push the current branch."

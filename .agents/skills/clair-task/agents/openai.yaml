interface:
  display_name: "Clair Orchestrator"
  short_description: "Delegate and integrate the Clair rewrite queue"
  default_prompt: "Use $clair-task run to work the Clair task queue autonomously until a real user decision or external credential is required."

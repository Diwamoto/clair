# NE-08: 任意差分の行・hunkモデルを作る

- 状態: done
- 優先度: P1
- 依存: [NE-02](NE-02.md)
- 担当範囲: 新規diffモデルとfixtureテスト
- 作成日: 2026-09-08

## 実装記録（2026-09-08）

任意のold/new文書を入力に取るエンジン非依存の行・hunk差分モデルを追加した。

- 実装: [`ProjectEditorDiffModel.swift`](../../../apple/ClairApp/ProjectEditorDiffModel.swift)
- 単体テスト: [`ProjectEditorDiffModelTests.swift`](../../../apple/ClairTests/ProjectEditorDiffModelTests.swift)
- Xcode登録: [`project.pbxproj`](../../../Clair.xcodeproj/project.pbxproj)
- 成果コミット: `9f8c4f5`

モデルはCRLF/LF、末尾改行なし、空文書、空行、Unicodeを論理行の本文と改行コードに分けて保持し、再構成時に表示用の合成行を挿入しない。行IDは文書ペアとrow index、hunk IDはold/new revisionを含めて決定的に生成する。rename metadataと本文差分は別フィールドに保持し、binary入力は本文行を作らずunsupported理由を返す。

`ProjectEditorDiffCoordinator`は差分計算をdetached taskへ移し、取消済みまたはgenerationが古い結果を破棄する。10,000行・2,000置換の実測テストも追加した。

実行結果:

- `ruby scripts/validate-xcode-project.rb` — pass（217 objects、4 targets、3 shared schemes）
- `CLANG_MODULE_CACHE_PATH=/private/tmp/clair-native-editor-module-cache SWIFT_MODULECACHE_PATH=/private/tmp/clair-native-editor-swift-cache xcodebuild -project Clair.xcodeproj -scheme "Clair Dev" -configuration Debug -derivedDataPath .build/xcode/ne08-debug -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO test -parallel-testing-enabled NO -only-testing:ClairTests/ProjectEditorDiffModelTests` — pass（7 tests、0 failures。10,000行/2,000置換テストは約0.60秒、取消・古い結果破棄を含む）
- 実行環境: macOS、Xcode 17F113 / SDK macOS 26.5、arm64。macOS host testとして実行。

## 目的と前提

任意差分の行・hunkモデルを作る。これはネイティブエディタPoCの残作業であり、本番への無条件採用は決定していない。
開始前に[共通ルールと基準](README.md)、[PoC報告](evidence/poc-report.md)を読む。NE-00完了後のソースを基準にし、依存Issueの成果コミットと完了条件を確認してから着手する。

## 実施手順

1. PoC alignedRowsと現行diff parserを読み、old/new文書revisionと論理行対応を保持するモデルを作る。
2. 追加・削除・置換・空ファイル・空行・末尾改行なし・CRLF/LF・Unicodeを扱う。表示用の空欄を本文に挿入しない。
3. 行/hunk IDを文書ペアとrevisionに紐付け、変更前後範囲と対応表を返す。
4. 10000行/2000置換などの負荷で計算時間を測り、バックグラウンド計算・取消・古い結果の破棄を実装する。

## 完了条件・検証

1. fixtureのold/new本文を行対応から復元でき、改行コードと末尾改行が失われない。
2. 同一revisionの再計算でIDが安定し、別revisionのIDを流用しない。
3. rename metadataと本文差分を分離し、binaryは非対応を明示。計算中もUIを塞がない。

## 対象外

Git stage/unstage、editable左右pane、diff UI。

## 引き継ぎ時に記録するもの

- 変更したファイルと成果コミットSHA（共有の指示なしにpushしない）。
- 実行したコマンド、環境、入力fixture、期待結果と実結果、証跡への相対リンク。
- 未検証・既知の制限。実行不能の場合は状態をblockedにし、不足している入力/環境を具体的に書く。
- 完了条件を満たした場合だけdoneに変更する。後続Issueの依存欄に共有可能な成果を追記する。

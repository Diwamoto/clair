# NE-11: 通常エディタを本番ホストへopt-inで接続する

- 状態: blocked
- 優先度: P0
- 依存: [NE-01](NE-01.md), [NE-02](NE-02.md), [NE-03](NE-03.md), [NE-04](NE-04.md), [NE-05](NE-05.md)
- 担当範囲: Native adapter、ProjectEditorTab接続、設定
- 作成日: 2026-09-08

## 依存ゲート棚卸し（2026-09-08）

NE-11の依存5件を、各Issueに記録された完了条件と突き合わせた。NE-02/04は完了しているが、NE-01/03/05のゲートが閉じているため、本番hostへの接続は開始しない。

| 依存 | 状態 | NE-11着手に必要な完了条件 | 判定 |
|---|---|---|---|
| [NE-01](NE-01.md) | `blocked` | 配布物ごとの固定ソース・許諾根拠・notice・binary対応を揃え、不明項目を配布不可ゲートとして解消する | 未達。通常版の配布許諾が `no` で、CodeEditLanguages/CodeEditSymbolsの再配布根拠が未解決 |
| [NE-02](NE-02.md) | `done` | revision/UTF-16範囲/atomic apply/snapshotの文書契約と既存保存経路への対応表を利用できる | 達成。NE-11で推測して契約を再定義する必要はない |
| [NE-03](NE-03.md) | `blocked` | 実IMEの入力・候補変更・確定/取消・再変換・タブ切替/保存を実操作し、marked-textのUndo境界を確認する | 未達。実IME未検証で、CodeEditTextViewのmarked-text更新がUndo履歴へ入る既知欠陥も残る |
| [NE-04](NE-04.md) | `done` | 6言語のquery/token色、編集後更新、revision gate、採用閾値とfallback条件を記録する | 達成。ただし大規模fileの本番適用判断はNE-05のゲートに従う |
| [NE-05](NE-05.md) | `open` | close後の解放測定、本文/選択/Undo復元、fallback/制限条件を完了条件どおり確定する | 未達。10MBで非同期parse/query jobのcancel・idle・解放が未証明で、native無条件投入不可 |

### 判定と停止範囲

- NE-11は `blocked` とする。NE-01/03/05を完了扱いにして依存を飛び越えない。
- 今回は本番hostのNative adapter、`ProjectEditorTab`接続、設定、Swiftテストを変更していない。
- 通常エディタ・diffの既定経路、保存、履歴、検索の既存経路も変更していない。
- 再開条件は、NE-01が配布許諾を解消して `done`、NE-03が実IMEとUndo境界を検証して `done`、NE-05が10MBを含む解放/fallback条件を完了して `done` となり、5件の成果SHAを引き継げることである。

### 検証記録

- `docs/issues/native-editor/README.md`、NE-01/02/03/04/05の状態・完了条件・記録を読み合わせた。
- 依存ゲートが閉じているため、本番実装のbuild、self-test、UI操作は実行していない。
- 今回の変更は本Issueの状態、依存棚卸し、停止範囲、再開条件、およびキューREADMEの整合性記録だけである。

## 目的と前提

通常エディタを本番ホストへopt-inで接続する。これはネイティブエディタPoCの残作業であり、本番への無条件採用は決定していない。
開始前に[共通ルールと基準](README.md)、[PoC報告](evidence/poc-report.md)を読む。NE-00完了後のソースを基準にし、依存Issueの成果コミットと完了条件を確認してから着手する。

## 実施手順

1. 依存Issueの成功SHAを取り込み、既存Web/native利用箇所と現在の未コミット作業との差分を確認する。
2. ユーザーが明示選択できる実験的native経路を追加し、既定は維持する。Issue02の文書契約に接続する。
3. 保存/Command-S、dirty、read-only、missing、外部変更検知、保存失敗、ローカル履歴、検索結果への移動を維持する。
4. タブごとにUndo/選択/スクロールを保持する。エンジン切替はIME終了と未保存本文保護を行い、変更を失う切替は拒否する。
5. 大規模fileの制限をIssue05に従って適用し、必要なSwiftテストをmake test-swiftで一回ずつ実行する。

## 完了条件・検証

1. 既存保存/外部変更/履歴テストと新adapterテストが通る。
2. 未保存編集を含む複数タブの往復、再読込、エンジン切替でデータ損失がない。
3. ライセンス未解決なら本Issueはblocked。通常版・diffの既定経路を変更しない。

## 対象外

既定native化、公開/配布、diffの置換。

## 引き継ぎ時に記録するもの

- 変更したファイルと成果コミットSHA（共有の指示なしにpushしない）。
- 実行したコマンド、環境、入力fixture、期待結果と実結果、証跡への相対リンク。
- 未検証・既知の制限。実行不能の場合は状態をblockedにし、不足している入力/環境を具体的に書く。
- 完了条件を満たした場合だけdoneに変更する。後続Issueの依存欄に共有可能な成果を追記する。

# NE-06: コメントanchorを編集・Undoに追従させる

- 状態: done
- 優先度: P1
- 依存: [NE-02](NE-02.md)
- 担当範囲: 新規コメントanchorモデルとテスト
- 作成日: 2026-09-08

## 実装記録（2026-09-08）

NE-02のUTF-16・revision契約に合わせ、本文とは独立したコメントanchor storeを追加した。

- 実装: [`ProjectEditorCommentAnchor.swift`](../../../apple/ClairApp/ProjectEditorCommentAnchor.swift)
- 単体テスト: [`ProjectEditorCommentAnchorTests.swift`](../../../apple/ClairTests/ProjectEditorCommentAnchorTests.swift)
- Xcode登録: [`project.pbxproj`](../../../Clair.xcodeproj/project.pbxproj)
- 成果コミット: （コミット後追記）

実装上の固定ルールは次のとおり。

- 編集範囲はdocument transactionと同じく編集前revisionのUTF-16座標で扱い、1 transaction内の複数編集も同じ座標系で写像する。
- anchor開始位置への挿入はanchor外、anchor内部への挿入はanchorを拡張、anchor終了位置への挿入はanchor外とする。本文検索による再接続は行わない。
- anchor全体を削除・置換した場合はorphan化し、Undo/Redoではtransaction履歴のbefore/after状態をそのまま復元する。Undo自体は文書側と同じく新しいrevisionとして記録する。
- snapshotはschema version、document ID、document revisionを持つ。IDまたはrevisionが一致しない再読込は拒否し、外部変更時は既存anchorをorphanとして返す。

実行結果:

- `ruby scripts/validate-xcode-project.rb` — pass（217 objects、4 targets、3 shared schemes）
- `CLANG_MODULE_CACHE_PATH=/private/tmp/clair-native-editor-module-cache SWIFT_MODULECACHE_PATH=/private/tmp/clair-native-editor-swift-cache xcodebuild -project Clair.xcodeproj -scheme "Clair Dev" -configuration Debug -derivedDataPath .build/xcode/ne06-tests -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO test -parallel-testing-enabled NO -only-testing:ClairTests/ProjectEditorCommentAnchorTests` — pass（4 tests、0 failures）
- 実行環境: macOS、Xcode 17F113 / SDK macOS 26.5、arm64。macOS host testとして実行。

## 目的と前提

コメントanchorを編集・Undoに追従させる。これはネイティブエディタPoCの残作業であり、本番への無条件採用は決定していない。
開始前に[共通ルールと基準](README.md)、[PoC報告](evidence/poc-report.md)を読む。NE-00完了後のソースを基準にし、依存Issueの成果コミットと完了条件を確認してから着手する。

## 実施手順

1. ファイルID、範囲、revision、コメントID、active/orphan状態をモデル化する。コード本文と別保存とする。
2. 挿入・削除・置換・複数編集transactionによる範囲移動を実装し、範囲の端への挿入時の所属ルールを明記する。
3. 対象削除でorphan化し、その削除をUndoしたとき同じ対象に戻せるようtransaction履歴と結び付ける。文字列検索だけで再接続しない。
4. ローカル保存/再読込のschemaを作り、文書revisionが一致しない場合に誤接続しない経路を実装する。

## 完了条件・検証

1. 対象前・内部・境界の編集、全削除、複数カーソル、Undo/Redo、保存再読込のテストが通る。
2. 同じ文字列が複数箇所にあっても別箇所へコメントが移らない。
3. 復元不能な外部変更はorphanとして表示用に返す。

## 対象外

クラウド共有、外部サービス同期、コメントUI。

## 引き継ぎ時に記録するもの

- 変更したファイルと成果コミットSHA（共有の指示なしにpushしない）。
- 実行したコマンド、環境、入力fixture、期待結果と実結果、証跡への相対リンク。
- 未検証・既知の制限。実行不能の場合は状態をblockedにし、不足している入力/環境を具体的に書く。
- 完了条件を満たした場合だけdoneに変更する。後続Issueの依存欄に共有可能な成果を追記する。

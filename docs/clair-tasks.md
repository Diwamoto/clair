# Clair 実行タスク一覧

Status: active execution queue
Date: 2026-09-22

仕様の正本は [`clair-spec.md`](clair-spec.md)。進捗の可視化は
[`clair-kanban.html`](clair-kanban.html)。この表が実行順序の正本であり、
`.agents/skills/clair-task/scripts/task_lease.py` が機械的に読む。

## 表の規約

- status は `next` / `queued` / `active` / `blocked` / `done` のいずれか。
- difficulty は `D1`〜`D5`。`D5` は独立レビュー必須。
- 行の書式(`| \`ID\` | \`status\` | \`D#\` | deps | outcome |`)は lease helper が
  parse する。崩さない。
- status と実行証跡を編集するのは controller のみ。worker は queue を編集しない。
- `done` は「統合済みブランチが完了条件を満たす」ことを意味する。worker ブランチ
  だけで満たした状態は `done` にしない。
- `blocked` は本物のユーザー gate か外部前提のみ。回復可能な実装失敗は `active`
  のまま残す。

## 優先度

- **P0** — v1 廃止に伴う整理。他のすべてに先行する。
- **P1** — 仕様の中核が実アプリで成立していない箇所。daily-driver の前提。
- **P2** — UI 一致と最終 QA。
- **P3** — 実機・人手 gate。外部依存があり日程を約束しない。

## 2026-09-21 の再編

ccedit(旧 Clair v1)を製品・資料ともに廃止した。これに伴い:

- `U08` を「v1 全削除」に置き換え、依存を外した(QA 待ちにしない)。旧 UI の
  cutover 判定は不要になり、単純な削除作業になった。
- `B04` を追加。コードから `v2` の命名を除去する。製品に v1/v2 の区別は無い。
- 仕様レビューで見つかった「core は実装済みだが実アプリに配線されていない」
  gap を `E11`/`E12`/`T09`/`N10` として明示した。これらは queue に存在しないまま
  「done 扱いの core」と「動かない実アプリ」の乖離を生んでいた。
- `E13`/`E14`/`V11` を追加。仕様 §5.11 と §9 が要求していて未実装の項目。
- `U03` の依存から `N08`(実機 dogfood gate)を外した。UI の実装が dogfood gate の
  完了を待つ理由はない。
- `V10` の依存から未定義の `G2` を外した。

## 2026-09-22 macOS dogfood review

実アプリの動作確認で見つかった問題を、症状だけで閉じず次の実行先へ割り当てた。

- App shell / navigation / motion は `U04`、editor chrome と切替性能は `U05`、terminal chrome と視認性は `U06` の残作業へ追加。
- syntax highlight は既存の `E11`、検索の main-thread stall と mock 乖離は既存の `V05`、Git sidebar / branch / pull / push は既存の `V06` の完了条件へ追加。
- Project tab と file tree の状態不一致、pane focus の遅延は同期 I/O を含む workspace state 問題として `V12` に分離。
- AI provider の残り使用量は表示だけでなくデータ源が欠けているため `H11` に分離。
- 動かない Debug navigation は見た目だけ直さず、仕様 §13 の DAP/Delve 実装として `V13` に分離。実装まで navigation を有効な操作に見せない。

## 残りタスク

| task | status | difficulty | depends | outcome |
|---|---|---|---|---|
| `U08` | `done` | `D3` | — | **P0**。v1 を全削除する。`apple/ClairApp`、`apple/ClairMobileApp`、`apple/ClairTests`、`apple/ClairTextKit`、`editor-web`、`Clair.xcodeproj`/`Clair.xcworkspace`、v1 専用 Makefile lane(`build-editor-web`、`build-stable`、`build-dev`、`run-stable`、`run-dev`、`test-swift`、`smoke-bundles` 等)、v1 専用 scripts、v1 前提の CI job を削除する。Rust crates(`crates/`、`Cargo.toml`、`test-rust`/`lint-rust`)は Swift 側が使っていないので同時に削除するが、削除前に `clair-ptyhost` に v2 が依存していないことを確認する。復元は archive tag からのみ。あわせて `README.md` の v1 前提の記述(`make run-dev`/`make test`/リポジトリ構成図/Rust CLI 説明)と `docs/runbooks/` を現状へ更新する。削除後に `make ci` に相当する経路が通ること。 **完了(2026-09-21、629337f)**: v1(`apple/` の v1 3 dir、`editor-web`、Clair.xcodeproj/xcworkspace、`crates/`、`Cargo.*`、`packages/ClairMobileKit`、v1 の `Config/*.xcconfig`、v1 専用 scripts 15 本、`stable-release.yml`)を削除。Makefile を v2 lane のみへ、`doctor.sh` から Rust を除去、README と runbooks を更新。`make lint`、`check` は通過、`test` は 224 件通過。`make ci` が通ることを確認済み。iOS の cross-build が落ちていた既存の不具合(`ClairWorkspace` の `Process`/FSEvents を `#if os(macOS)` で分離)と、不安定だった `EditorBenchmarkTests` の noop 計測を修正した。v2 の署名 update 用 release workflow は `V09` で新設が必要(`generate-update-*.swift` は残置)。 |
| `B04` | `done` | `D3` | `U08` | **P0**。コードから `v2` の命名を除去する(仕様 §4)。`ClairV2Core`→`ClairCore` 等の package/target/module、`ClairV2*` の型名、`clair-v2-*` のファイル名、`ClairV2Mobile.xcodeproj`、`v2-*` の Makefile target と scripts、`CLAIR_CHANNEL` 以外の v2 由来の識別子。bundle ID と data 領域(`Clair v2`/`Clair Dev v2`)も改名するため、既存 workspace.json の移行経路を用意する(ADR-0008 の identity 契約を壊さない)。機械的な rename を 1 コミットにまとめ、意味のある変更を混ぜない。 **完了 (2026-09-21)**: `ClairV2*`→`Clair*`(package `ClairCore`/`ClairApps`、全 target・型・ファイル)、`ClairV2Mobile.xcodeproj`→`ClairMobile.xcodeproj`、`v2-*` の scripts と Makefile target(`build`/`test`/`check`/`foundation` 等)、`CLAIR_V2_*` 環境変数を rename。data 領域は ADR-0008 どおり `Clair`/`Clair Dev` に戻し、旧 `Clair v2`/`Clair Dev v2` の `workspace.json` を初回のみ移す `ClairChannel.migrateLegacyWorkspace`(既存を上書きしない、test 付き)を追加。bundle ID は元から `v2` を含まず変更なし。**据え置き**: Keychain service `com.diwamoto.clair.mobile.v2`(改名すると保存済みの pairing 秘密が孤立するため。移行は別タスク)。**検証**: `make lint`/`check`/`build`/`mobile-build`/`build-mobile-simulator` 通過、`ClairApps` test 通過。`testCutDeletesSelectionAfterWritingToPasteboard` は並列実行時のみ失敗し、rename 前の base でも同じ失敗(既存の flake)。ディスク上の `.cache/v2-ghostty` は `.cache/ghostty` へ移す必要あり。 |
| `T10` | `done` | `D3` | `T03` | **P2**。macOS terminal surface(`ClairGhosttySurfaceView`)に日本語 IME を配線する(仕様 §7 の `NSTextInputClient`/marked text 要求、`T06` が誤って `done` の scope に含めていた gap)。現状は `NSTextInputClient` 非準拠で `keyDown` が生イベントを ghostty へ直接送るだけ(`ClairGhosttySurfaceView.swift:364` の `ponytail:` 注記)。英数字はそのまま通るが IME 変換は一切効かない。2026-09-24 の実アプリ動作確認(owner)で日本語入力不可・英語入力は問題なしを確認。`NSTextInputClient` 準拠と `ghostty_surface_preedit`(または同等の marked text 経路)を実装し、変換確定を 1 回の PTY write にまとめ、Escape でのキャンセルを扱うこと。E07 の macOS editor 側 `NSTextInputClient` 実装を参考にできるが、terminal は行ローカルの文書モデルを持たないため流用ではなく個別実装になる。 **完了(2026-09-24、cefdeff)**: macOS surface を `NSTextInputClient` に準拠させ、変換中文字を `ghostty_surface_preedit` へ表示、確定文字列を 1 回の key event として送信。Escape での変換キャンセルは PTY へ送らない。Ghostty の IME 候補位置を AppKit に返す。実 Ghostty リンクの `ClairAppKit` build と `ClairGhosttySurfaceViewTests` 9 件通過。**残**: 実アプリの Claude 上で日本語変換の目視確認。 |
| `T09` | `done` | `D5` | `T02`, `T04`, `T03` | **P1**。Mac GUI の terminal を daemon 所有 session へ付け替える(仕様 §7)。現状 GUI は `ClairLocalShellSession` で自前 PTY を持ち、`ClairMacApp` は daemon を起動も接続もしていないため、「Mac と mobile は同じ session の別 surface」が実アプリで成立していない。GUI から daemon の single-instance を起動・接続し、surface を `attach`/`detach` で session に繋ぎ、window を閉じても session が継続すること。既存の journal/epoch/cursor/gap-resync(`H08`/`T04`)をそのまま使い、GUI 専用の側路を作らない。Mac と mobile の同時入力が同じ PTY に到着順で適用されること。D5 独立レビュー必須。**完了(2026-09-21)**: daemon が login shell を所有し(`ClairLocalTerminalHost`)、agent session と同じ `ClairTerminalBoundary`(journal/epoch/cursor/gap/入力 FIFO)に載せた。control socket に same-user 限定の terminal open/read/input/resize/close を追加(peer uid 検査)、Ghostty surface の child は `clair attach`、GUI は daemon を起動し pane.close で shell を終了、quit で daemon 停止。GUI は PTY を持たない(`ClairLocalShellSession` は daemon 側へ移動、surface の `session` は test 専用の任意引数)。D5 独立レビュー(opus。fable が利用上限のためユーザー承認の下で代替)で BLOCKER 1 / MAJOR 3 を指摘 → 修正 → 再レビュー APPROVE。test 8 件(detach/reattach、Mac+paired mobile の同一 PTY と入力順、4200 入力、slot 解放、cwd/command 不一致、exit 後の reopen、resize/close、拒否)+ integration 108 件 + iOS build 通過、実バイナリ e2e で detach/再 attach 後も同一 pid。**残(別タスク/follow-up)**: 実 GUI での目視確認、mobile からの shell 発見・grant(`N10`/`T07`。default grant に `local-terminal` は含まれず、closed pane は mobile 側で staleSession になる)、update 再起動時の reattach(`V09`。現状 quit は daemon 停止)、long-poll が GCD worker を占有、既存事項として遠隔入力は session あたり 4096 op で頭打ち(boundary の ledger)。 |
| `E11` | `done` | `D4` | `E05`, `U05` | **P1**。syntax highlight を実ファイルで色として出す(仕様 §5.11)。現状 `SyntaxParser`(E05)には `ClairEditorLanguage` の外に呼び出し元が無く、`ClairEditorView.highlights` は常に空。日常的に使う言語の tree-sitter grammar を vendor し(Swift、Go、TypeScript/JS、Python、JSON、Markdown、Rust、shell を最低線)、parse 結果の capture を `EditorTokenKind` へ対応づけ、`EditorBuffers` から view へ渡す。`INV-PERF-005`(cancellable background、入力を止めない、欠けても正しさは損なわれない)と `INV-REV-002`(属性変更は revision を進めない)を満たすこと。差分 parse で 10 MiB fixture の打鍵が回帰しないことを計測する。**dogfood review (2026-09-22)**: 実アプリで highlight がまったく色として出ないことを再確認。fixture だけでなく Project から開いた各対象言語の実ファイルで capture→token→描画を通す UI regression test と目視確認を完了条件にする。 **性能計測 (2026-09-22、[結果](benchmarks/results/2026-09-22-Mac16,10/README.md))**: `make perf-budget` が `syntax.reset`/`syntax.update` を「UI 側に呼び出し元が無い」として失敗させている。実測では 4 MiB JSON の初回 parse が 175.8 ms、差分 parse が 4.13 ms なので、cancellable background + 差分 parse なら `BUDGET-OP-100` を満たせる。配線後に当該 2 件が `background` 以上になることを完了条件へ追加する。 **完了(2026-09-23、77dd618)**: Swift/Go/TypeScript/JavaScript/Python/JSON/Markdown/Rust/shell の tree-sitter grammar を本番 target として vendor(`ClairEditorLanguageFixtures` の test-only パターンを踏襲、各 `VENDOR.md` に出典・commit・改変点を記録)。拡張子/shebang から language を判定し(未対応拡張子は無色のまま開く、crash しない)、capture 名を `EditorTokenKind` へマップ(未知 capture は `.plain` fallback)。`SyntaxHighlightActor`(actor)が file ごとに `SyntaxHighlighter`/`SyntaxParser` を所有し、`EditorBuffers` が cancellable `Task` でラップ(file 切替/close で cancel)。`SyntaxParser.update` の差分 parse のみを使用、attribute 変更は revision を進めない。`make perf-budget`: `syntax.reset.4mb-json` は `background+loading`(179ms、`highlights` affordance に `ProgressView` 併設)、`syntax.update.4mb-json` は `background`(4ms)、`editor.keystroke.*` 回帰なし。real snippet(8 言語)での capture→token→span 生成を `EditorHighlightingRegressionTests` で検証。`make check`/`build`/`test`/`lint`/`perf-budget` 通過。**残**: (1) 実アプリでの目視確認は worker 環境に screenshot 手段が無く未実施(自動テストでの capture→span 生成確認まで)。次回起動時に Project で各言語の実ファイルを開いて確認すること。(2) `.tsx` は TSX 専用 grammar でなく通常の TypeScript grammar を使用(JSX 構文で誤 parse の余地)。(3) Markdown は block grammar のみ vendor、inline(強調/リンク等)は非対応。(4) tree-sitter query の `#eq?`/`#match?` predicate は未評価(`QueryCursor.nextCapture` を直接使用、predicate フィルタなし)。(5) capture 名は先頭ドット区切り要素のみでマッチ(例 `@keyword.return`→`.keyword`)。 |
| `E12` | `done` | `D5` | `E05`, `E11` | **P1**。LSP を実アプリに配線する(仕様 §5.11、§9)。現状 `LSPDocumentSession` の呼び出し元は test だけで、language server を起動する経路が無く diagnostic も補完も出ない。server の起動・停止・crash 再起動、`ClairEditorView.diagnostics` への表示、補完、定義ジャンプ、参照検索、symbol 検索(palette の symbol 到達)を実装する。最初の第一級は gopls(仕様 §13)、それ以外は汎用経路で動けばよい。revision で stale な結果を拒否すること(`INV-REV-004`)。D5 独立レビュー必須。 **進捗(2026-09-23)**: `93ca66a` で実装。`LanguageServerClient`(actor)が Project root×server ごとに起動・initialize・増分 didChange・gopls が待つ server request への応答・crash 再起動(全文書を最新 revision で再 open、60 秒に 3 回で停止し status に表示)・stop を持つ。通知は直列化し request は通知の書き込み完了後に組むので didChange を追い越さない。位置付き応答は revision 不一致で破棄、診断は UI 側でも view の revision と照合し、次の publish までは編集に沿って rebase(`INV-REV-004`)。gopls が第一級、ts/pyright/rust-analyzer/sourcekit-lsp は同じ経路(login shell PATH + `~/go/bin`、main 外で解決)。UI: 波線 + hover で message、補完 popup(trigger 文字・識別子入力・Esc/⌃Space、↑↓/Return/Tab/Esc、確定は通常の edit 経路で 1 undo)、`editor.definition` ⌃⌘J、`editor.references` ⌃⌘R(palette 一覧)、`palette.symbols` ⌘T(workspace/symbol)、status bar に server 状態とエラー/警告数。検証: 実 gopls v0.23.0(`/tmp` に一時構築)で統合 4 件(revision 別診断・補完・定義・参照・symbol・kill -9 後の復帰・fake server で stale 補完破棄と crash loop 停止・アプリ配線で rebase→`.` 補完→Return 確定→同 revision で再診断→定義)、unit 3 件追加、`make test`/`make build` PASS、rebase を外す mutation で統合 test が落ちることを確認。**残**: D5 独立レビュー、実アプリ目視(gopls が login PATH に無い)、他ファイル変更の `didChangeWatchedFiles` 通知は未実装。 **完了(2026-09-23)**: D5 独立レビュー(opus)で BLOCKING 1 件(⌘T で古い query の `workspace/symbol` 応答が新しい結果を上書き)→ `c3adfae` で `Task.isCancelled` を検査して破棄 → 再レビュー approved。framing/UTF-16/didChange 順序/stale 拒否/crash 再起動/main 非ブロック/補完 1 undo は問題なしと確認。**follow-up(非 blocking)**: 実アプリ目視は `U07`、`didChangeWatchedFiles` 未実装、`stop()` は Project close 導入時に配線、version 無し server の診断は素通し、request timeout 無し、標準ライブラリへの定義ジャンプで Project と gopls が増える(V11 `file.open` の挙動、製品判断待ち)。 |
| `V09` | `blocked` | `D4` | `H01`, `T02`, `T09` | **P1**。Stable/Dev identity、署名 update、sleep 抑止は実装済み(test 7 件)。**残**: update restart 時の PTY reattach。これは GUI が daemon 所有 session を使うことが前提なので `T09` の後に行う。実 `.app` への適用確認も残る。 **進捗(2026-09-23)**: `078f50d` で update restart 時の PTY reattach を実装。shell は daemon 所有(`T09`)なので、`installUpdate` は workspace を同期 flush してから `ClairDaemonLauncher.keepsSessionsOnQuit` を立てて終了し、`applicationWillTerminate` は daemon を止めない。再起動後の app は同じ pane id を復元し `project#pane` キーで既存 shell へ `clair attach` する(通常の quit は従来どおり daemon 停止)。検証: `ClairLocalTerminalHostTests` 8 件(detach→同 key reattach で同一 session)、`WorkbenchUpdateTests` 8 件、`swift build`(ClairCore/ClairApps)PASS。**Blocked**: 残る実 `.app` への適用確認は Ed25519 署名済み GitHub Release の公開と `/Applications` への install が必要(publish = ユーザー gate)。既知の上限: 旧 daemon は次の明示 quit まで旧版のまま動く。 |
| `V03` | `done` | `D5` | `V01`, `V02` | **P1**。`clair mcp serve` の stdio MCP adapter。実装と threat test(4 件 + IPC 4 件)は完了。**完了(2026-09-23)**: D5 独立レビュー(opus)で BLOCKING 2 件 → `b3a2dee` で修正 → 再レビュー approved。(1) 承認後の実行は同じ main actor turn で live state を再検査し、project/active tab/focused pane が承認時から動いた、または再 preflight の risk が承認値を超えたら `denied`。`confirmed: true` は承認 risk が destructive 以上のときだけ。(2) tools/call の arguments は CFBoolean で bool を判定(0/1 が bool 化していた)、null/配列/object は `-32602`。threat test 6 件 + IPC 4 件 pass、recheck 無効化の mutation で 3 件 fail。前提と既知事項は `docs/plans/clair-v03-mcp.md`(`via` は自己申告で同 uid は迂回可能だが権限拡大ではない、stdio 行長無制限、IPC 直列で承認待ち中は CLI が待つ、承認カードに暗黙の対象が出ない)。 |
| `V06` | `done` | `D5` | `V01`, `V04`, `H07`, `E09` | **P1**。Git/worktree workflow。stage/commit/switch、managed worktree、branch review(committed/uncommitted/untracked 分離)を実装済み。conflict は abort して agent へ再依頼(原則 6 が許す経路)。**dogfood review (2026-09-22)**: Git sidebar、branch picker、pull/push を実用配線。Git なし Project では navigation を無効化。**進捗 (2026-09-23)**: 隔離 branch `codex/v06-git-ui` の `a076ec7..39815ce` は D5 独立レビュー approved。status/diff/stage/unstage/commit、branch 一覧・切替、typed pull/push、進行中/成功/失敗/認証案内、未保存 buffer / closed-tab cache / 遅延 FSEvents / deleted path / managed worktree の回帰修正をメイン作業ツリーへ手動統合し、関連 21 tests と `make build` PASS。**完了(2026-09-23)**: worker range の変更(追加 test 8 件・関数)は `rewrite/clair-v2` に統合済みで作業ツリーはクリーン。`WorkbenchGitTests`/`EditorBuffersTests` 21 件 PASS を再確認し lease `648d4cc8` を release。**夜にユーザー確認**: native UI 手動操作と実 HTTPS/SSH 認証経路(ユーザー指示で実機確認待ちは後続を止めない)。 |
| `V14` | `done` | `D4` | `V06` | **P1**。main thread を止める同期処理を予算内へ戻す(`BUDGET-OP-100`)。2026-09-22 の計測(Release、M4、[結果](benchmarks/results/2026-09-22-Mac16,10/README.md))で 3 経路が 100 ms 予算を超えた。(1) `WorkbenchState.refreshStatus()` p95 225.9 ms — `git.stage`/`git.commit`/`git.switch` の**毎回**、全走査 + `git status` を main actor で同期実行する。(2) `WorkbenchState.review(base:)` p95 112.7 ms — `git` subprocess 3〜5 本を同期実行。(3) `LocalHistory.preview` p95 123.4 ms — 20 万行 × 2 の `CollectionDifference` で、行数に比例して上限が無い。あわせて `WorkbenchGit.run` の `waitWithoutRunLoop()` に timeout が無い既存の穴(`ponytail:` 注記)を塞ぐ。予算を緩めるのではなく background へ移して解消すること。完了条件は `make perf-budget` の当該 3 件が `pass` になり、Git 操作の意味(argv 配列・conflict abort・`isRepo` gate)が変わらないこと。 **完了(2026-09-23)**: 3 経路は V06 統合時の変更で既に解消済みだった(`refreshStatus` は全走査をやめ `git status` のみ、UI の stage/commit/switch/pull/push は `performGitFromUI` の detached task、`LocalHistory.preview` は `HistoryList` の detached task + `loading` 表示)。`make perf-budget`(Release、[結果](benchmarks/results/2026-09-23-Mac16,10/budget.json))で `git.refreshStatus.dirty` p95 26.0 ms pass、`git.review.dirty` 43.7 ms pass、`history.preview.10mb` 130.1 ms `background+loading`、suite 全体 PASS。残っていた timeout の穴は `Process.terminate(after:)`(pid へ SIGTERM)を `WorkbenchGit.run` と `WorkbenchFiles.gitStatus` に入れて塞いだ(上限 120 s、中断時は理由付きの失敗を返す)。argv 配列・conflict abort・`isRepo` gate は不変。`WorkbenchGitTests`/`EditorBuffersTests`/`WorkbenchProjectTests` 36 件 PASS(hung child を deadline で止める test 追加)。既知: palette/CLI/MCP 経由の `git.*` は従来どおり main actor で同期実行(`git.review` 44 ms で予算内)。 |
| `V15` | `done` | `D3` | `V04`, `U05` | **P2**。100 ms を超える background 処理にローディング表示を出す(`BUDGET-OP-100`)。同計測で 2 経路が「background だが画面に何も出ない」状態だった。(1) `WorkbenchFiles.scan` p95 960.4 ms(20,000 ファイル)/ 226.8 ms(dirty) — `ClairAppShell.diskChanged` は utility queue で正しく走るが `scanning` が private `Bool` のままで、起動直後とプロジェクト切替後に file tree が約 1 秒間空のまま無言になる。(2) `EditorBuffers.prefetch` p95 482.9 ms(10 MiB) — detached task は正しいが読み込み中のタブが何も出さない。既に正しい型は repo 内にある(project search の `searching` → `ProgressView`)ので、新しい仕組みを作らずそれに合わせる。完了条件は `make perf-budget` の当該 2 件が `background+loading` になり、実アプリでの目視確認。 **完了(2026-09-23)**: 実装は `b22ad78` に同梱済みだった(explorer は `ClairWorkbenchStore.scanning`(`@Observable`)→ `ProgressView`「ファイルを読み込み中…」、editor は `buffers.peek == nil` の間 `ProgressView`)。起動時は `watchProject(refresh: true)`、Project 切替時は `watchProject()` の両経路で `scanning` が立つことをコードで確認。`make perf-budget` 再実行(Release、他セッション稼働中で p95 は高め): `tree.scan.20k` 2649.6 ms / `tree.scan.dirty` 272.2 ms / `file.rope.10mb` 1441.4 ms がいずれも `background+loading`。suite の失敗は `E11` 未実装の `syntax.*` 2 件(`missing-affordance`)のみで前回記録と同じ。実アプリ目視: 隔離 workspace(`CLAIR_CHANNEL=dev`、`CLAIR_WORKSPACE_FILE`/`CLAIR_PROJECT_ROOT` を /tmp)で 20,000 ファイル corpus を開くと tree 上部にスピナー付き「ファイルを読み込み中…」→ 走査後に消えて tree 表示、9.5 MB ファイルのタブ復元時は editor pane 中央にスピナー表示を screenshot で確認。 |
| `V05` | `done` | `D4` | `V01`, `E04`, `V04` | **P1**。Quick Open、全文検索・置換、FSEvents watcher、local history を実装。**dogfood follow-up 完了(2026-09-22)**: 検索を titlebar の typed command (`palette.search`, `⌘⇧F`) から開く Workbench mock 準拠の overlay へ統合し、query、進行中、file group、件数、empty/error、keyboard/hover 選択、file/line 遷移を実装。検索は 250 ms debounce + cancellable detached task と generation guard で古い結果を拒否し、一括置換も別の detached task で UI を止めない。重複していた sidebar の検索 destination は削除。`WorkbenchCommandTests` / `WorkbenchSearchTests` 13 件通過、10,000 files の scan 0.371s・rank 0.006s、native snapshot で overlay を視覚照合。 |
| `V11` | `done` | `D3` | `V01`, `V02` | **P1**。仕様 §9 の未実装分。(1) shortcut を任意 command へユーザーが割り当てられるようにする(現状 shortcut は registry の固定 projection)。(2) `clair open path:line:column` を実装する(現状 CLI は `mcp serve` のみ)。path を所有する open Project の active pane へ開き、該当 Project が無ければ新規 Project として開く。 **完了(2026-09-21)**: (1) `file.open`(absolute path + 任意 `line`)を registry に追加。path を所有する open Project(入れ子は最深の root)へ切り替えて tab を開き、無ければ最寄りの Git root(無ければ file の folder)を新規 Project として開く。scan 対象外(`node_modules` 等/上限超)の file も開ける。`ai: false`(agent が読める範囲を勝手に広げない)。`clair open path:line:col` はこれに配線し、相対 path は CLI 側の cwd で絶対化する。line は editor の reveal へ渡す。(2) `shortcut.set`(command, shortcut)を追加。割り当ては `WorkbenchState.shortcuts` に持ち保存・復元され、メニューとパレットの hint は registry 既定ではなく有効な shortcut を投影する。`""` で既定を解除、`⇧⌘x`→`⌘⇧X` に正規化、⌃⌥⌘ を含まない key・重複・引数必須の command・未知の command は拒否。`ai: false`。test 追加(owner/入れ子/Git root/folder、不正入力、割り当て・衝突・解除・保存)。`make lint`/`build`/`test`/`mobile-build` 通過。**残**: col は editor の reveal が line 専用のため未反映、shortcut の割り当て UI(設定画面)は無く CLI/`shortcut.set` 経由のみ、実 GUI での目視確認。 |
| `E14` | `done` | `D3` | `E03`, `U05` | **P2**。multi-cursor の UI 手段を仕様 §5.5 の水準に上げる。core(`TextSelectionSet`、`TextSelection.rectangular`、1 undo 単位)は実装済みだが、UI からは ⌘クリックの cursor 追加しか使えない。⌘D(次の一致を選択)、⌥ドラッグの矩形選択、ダブル/トリプルクリックの語/行選択を追加する。あわせて editor pane の file サイズ上限(現 10,000,000 bytes)を canonical fixture `10mb`(10,485,760 bytes)が開ける値へ直す(仕様 §5.9)。 **完了(2026-09-23)**: `ClairEditorView+MultiCursor.swift`。ダブルクリック=AppKit 標準の語境界(`NSAttributedString.doubleClick(at:)`)、トリプルクリック=改行込みの行、⌘併用で既存選択を保持。⌥ドラッグは各行に同じ x 範囲を当てる視覚列の矩形選択で、全角/cluster 内側の端は左 down・右 up に外側 snap(`INV-MC-004`)、短い行は行末 cursor。⌘D は cursor のみなら語選択、選択ありなら次の一致を追加(末尾で先頭へ wrap、全部選択済みなら不変)して scroll。選択変更は edit ではないので undo 履歴に入らない。上限: `10mb` fixture は行単位生成で 10,485,776 bytes と 10 MiB を超えるため `EditorBuffers.maxBytes` を 16 MiB にし、文言も上限から生成。`ClairEditorMultiCursorTests` 5 件(実 mouse handler 経由。snap 補正を外すと矩形 2 件が落ちることを mutation で確認)+ 既存 `ClairCoreTests` 計 225 件通過。**既知の上限(ponytail)**: ダブルクリック後のドラッグは語単位でなく文字単位、矩形ドラッグは跨いだ全行を毎回 layout、⌘D は最悪で文書全体を走査。実アプリ目視は画面ロック中のため未。 |
| `E13` | `done` | `D4` | `E05`, `E06`, `E11` | **P2**。code folding と soft wrap(仕様 §5.11)。どちらも core/view に実体が無い。fold 範囲は tree-sitter の構文範囲から導出し、fold state は `INV-TXN-003` の position mapping を通して編集に追従させる。fold された行をまたぐ caret 移動・検索・review anchor の扱いを決めること。soft wrap は `INV-PERF-001`/`003` を壊さない(長い 1 行も viewport 制限経路で扱う)。minimap は対象外(仕様 §14)。 **完了(2026-09-23)**: `76a812f`/`f0ae151`。canvas/mock に fold・wrap の UI が無いため、ユーザー承認の下で ccedit(`archive/clair-v1-2026-09-14`)の挙動を仕様として採用: gutter の fold 列に `⌄`/`›`、折りたたみ行末に `⋯` pill、`editor.fold`/`editor.unfold` ⌥⌘[ / ⌥⌘]、`editor.foldAll`/`editor.unfoldAll` ⌃⌥[ / ⌃⌥]、設定 › エディタ「行の折り返し」(既定 OFF、`editor.toggleWrap` ⌥Z、文字単位で等幅グリッド・全角 2 桁)。fold 候補はハイライトと同じ背景 tree-sitter pass でヘッダ行ごとの最大の複数行ノードを取り、計算した revision にだけ適用。fold は byte 範囲で全編集の position mapping を通り(`INV-TXN-003`)、隠れた本体に触れる編集で破棄、選択が隠れ行に入ると(caret・検索 hit・review anchor・定義ジャンプ)開く。閉じ括弧行は残す。`EditorRowMap` が文書行↔表示行を fold 数+折り返し行数の O(log n) で写し(行数に比例しない)、描画・hit test・caret・選択・診断・reveal・IME 候補位置・a11y・↑↓ がすべて経由。wrap の再計測は編集行のみ、全走査は wrap ON/幅変更/内容置換時だけ。検証: `ClairEditorFoldWrapTests` 7 件(行 map、全角/絵文字の折り返し、fold の隠し・↓で飛ばす・reveal で開く、上方編集で追従・本体編集で破棄、gutter click/⌥⌘[、wrap の行数・hit・caret・↓・編集後の再計測、Go の構文木から fold 候補)、`make test` 2 回連続 PASS、`make perf-budget` 全項目 pass(`syntax.update.4mb-json` 4.27ms、keystroke 系は不変)、`make build` PASS。あわせて copy/cut/paste テストを専用 pasteboard に変更(`--parallel` でユーザーのクリップボードを奪い合っていた)。**残**: canvas/mock への fold・wrap 状態の後追い反映(`Artifact` が使える session で `clair-workbench-sync`)、実アプリ目視、iOS view は未対応、IME 変換中の行は wrap せず 1 行で描画、⌥ドラッグ矩形は各行の先頭表示行基準、数 MB の単一行は編集ごとに 1 回全体を測る。 |
| `U04` | `done` | `D3` | `U02`, `E07`, `T03` | **P2**。macOS AppShell、titlebar、sidebar、pane/tab、status bar、command/settings を Workbench mock に合わせる。**dogfood follow-up 完了(2026-09-22)**: window chrome を一段へ統合し、偽 traffic light を除去、Project tab の間隔と hover fade、left navigation の hover/selection、未実装 Debug の disabled/準備中表示、settings destination の token 準拠 cross-fade(reduced-motion 時は即時)を実装。native build 成功後、実アプリで二重 titlebar/traffic light が再現しないこと、File / Git / Terminal navigation が内容を切り替えること、Debug が無反応な有効操作に見えないこと、一般→エディタ→ターミナルの連続切替が即時反映されることを目視・Accessibility tree で確認。quota は `H11`、highlight は `E11` へ分離。 |
| `U05` | `done` | `D4` | `U04`, `E07`, `E09`, `V01` | **P2**。Mac の editor / diff / review / context menu を mock に合わせる。slice 11 まで完了(実ファイル接続、変更一覧+stage、diff pane、review thread 永続化、hunk ナビ、agent へ送る、コミット欄、行ドリフト追従、1 行 suggestion)。**dogfood follow-up 完了(2026-09-22)**: editor scroll indicator を track/background なし・角張った token 準拠 thumb の overlay scroller へ変更。file/buffer、diff parse、history preview/restore、review 永続化、UI 起点の save を background 化し、切替直後は breadcrumb/loading を表示、連続切替・drop・Project 変更時の cancel/stale result rejection を実装した。10 MiB fixture を canonical 10,485,760 bytes まで許可し、連続 a/b/a/b 切替の main-actor scheduling は 0.2 秒未満、5,000 行 diff parse は 0.013 秒、20,000 file explorer model は 0.050 秒。対象 test 13 件、全体 build、native snapshot が通過。実アプリで README→Makefile→README の即時切替、scroll thumb の移動、保存後の継続動作を確認し、同時に見つけた非同期 workspace 保存の actor isolation crash も回帰 test 付きで修正した。highlight は `E11`、multi-cursor UI は `E14` へ分離。 |
| `U06` | `done` | `D3` | `U04`, `T03`, `V01` | **P2**。Mac の terminal / session list / 承認面を mock に合わせる。通知履歴、session list、承認カード、status bar 実データ化まで完了。描画性能は flood 下の main stall が overlay 有無で 5.3→5.7ms で悪化なし(2026-09-21 計測、`ClairGhosttyFloodPerfTests`)。**dogfood review (2026-09-22)**: (1) terminal 全体が選択されたように見える focus/cursor 表現を止め、非 active pane の caret 点滅を止める、(2) ANSI palette/foreground/contrast が薄いので mock と可読性基準へ合わせる、(3) panel header の terminal title/label を外し、三点リーダの drag handle を上端中央へ置く、(4) Agent 専用 panel は削除し、agent は raw terminal session として terminal panel に統合する。描画・選択・IME・copy/paste の意味は変えない。**残**: 上記修正、実機目視、GPU frame time 計測。 **進捗(2026-09-22)**: panel header の label を除き drag handle を中央へ。**進捗(2026-09-23)**: caret 点滅の根本原因は `viewDidMoveToWindow` で `setFocus(true)` を一度送るだけで false を送らず全 pane が focused 扱いだったこと。poll で key window の first responder のみ focused に同期し、非 active pane の caret 点滅を停止(`swift build`/Ghostty 系 16 tests 通過、実機目視は未)。**残**: terminal 全体が選択されたように見える focus 表現、ANSI palette(ghostty config へ palette を渡す API が未整備)、Agent 専用 panel の統合、GPU 計測。 **進捗(2026-09-23, 2)**: (1) ANSI palette: `36b1bfb` で libghostty に Clair 専用 config file(ユーザーの ~/.config/ghostty は読まない)を load+finalize する ABI を追加し、mock の One Dark(bg `#282c34`/fg `#abb2bf`/palette は tokens の code 色)+ `minimum-contrast = 3` を適用。実 surface で diagnostics 0 と shell 描画を確認する test を追加(不正キーを入れる mutation で fail)。(2) Agent panel 統合: `c7646b3` で `PaneKind.agent` と空の Agent pane を削除し、既定レイアウト右上を terminal に。保存済み `agent` leaf は terminal として復元(test 追加)。agent 起動は palette の「〜を起動」から。検証: Ghostty 系 15+11 件、Workbench/PaneTree 74 件 pass。**残**: terminal 全体が選択されたように見える focus 表現(画面ロック中で原因の目視特定ができず未着手)、実機目視、GPU frame time 計測。 **進捗(2026-09-23, 3)**: focus の真実を store の focused pane 1 本に統一。原因は (a) terminal クリックを Ghostty NSView が受けて SwiftUI の `onTapGesture` に届かず store focus/dim(非 active opacity 0.75)が追従しない、(b) 全 surface が mount 時に `makeFirstResponder(self)` し最後の pane が keyboard を奪い store と食い違う、の 2 点。surface は `becomeFirstResponder` で store へ報告し、store focus の変化時だけ first responder を取る(caret 点滅は既存 poll が first responder のみ focused にする)。test `u06OnlyTheStoreFocusedSurfaceTakesTheKeyboard` 追加、Ghostty surface 系 8 件 pass。**残**: 実機目視(複数 terminal で caret/dim が 1 pane だけ active になるか)、GPU frame time 計測。 **完了扱い(2026-09-23, owner判断)**: `df9c638` の focus 修正と Ghostty surface 系 8 件の成功をもって U06 を done。複数 terminal の caret/dim 実画面確認と GPU frame time 計測は owner が後日確認する方針で、U07 の Mac QA に引き継ぐ。 |
| `U09` | `done` | `D2` | `U04` | **P1**。ターミナル/エディタ pane の境界(分割線)をドラッグして pane の大きさを変えられるようにする(2026-09-23 owner 依頼)。現状の分割線は 1pt の SwiftUI `DragGesture` で、隣の libghostty / editor の NSView に hit-test を奪われて実質掴めず、resize cursor も出ない。さらに `setRatio(splitContaining:)` が「first 側に id を含む split すべて」を書き換えるため、入れ子分割では外側と内側の比が一緒に動く。 **完了(2026-09-23)**: 分割線ごとに AppKit の `SplitHandle`(7pt の hit strip、`resizeLeftRight`/`resizeUpDown` cursor、window 座標の差分で比を更新、`acceptsFirstMouse`)を両 pane の上に重ね、terminal/editor の NSView 上でも掴めてドラッグが途切れないようにした。`setRatio(splitContaining:)` は「first 側の最後の pane が id の split」だけを対象にし(一意)、入れ子の分割線が連動しないようにした。`testRatioMovesOnlyThatDivider` 追加、`PaneTreeTests`/`WorkbenchCommandTests` 15 件と `ClairMacApp` build 通過。**残**: 実アプリでのドラッグ目視(統合時は他タスクが Clair Dev daemon を使用中で起動できず)。 |
| `U10` | `done` | `D2` | `U04` | **P1**。アプリアイコンを正しく設定する(2026-09-23 owner 依頼)。v1 削除(`U08`)で `apple/ClairApp/Assets.xcassets` の `AppIcon`(dark)/`AppIconDev`(yellow Dev ribbon)が消え、SwiftPM 実行ファイルの `ClairMacApp` は Dock に汎用アイコンを出している。旧資産を復元し、channel(`ClairChannel.current`)に応じて Stable/Dev のアイコンを出す。 **完了(2026-09-23、eff91ba)**: `629337f^` の `AppIcon-512@2x.png`/`AppIconDev-512@2x.png`(1024px)を `ClairMacApp/Resources` へ復元し SwiftPM resource 化。起動時に `ClairChannel.current` で選び、macOS の icon grid(824pt 本体・角 185pt)に切り抜いて `NSApp.applicationIconImage` に設定。resource accessor で target が複数ファイルになり `main.swift` の `@main` が不可になるため `ClairMacApp.swift` へ rename。build・`make check`・PackageBoundary test 通過。built bundle から同じ描画で両アイコンを書き出して目視確認(実 Dock の screenshot はディスプレイ停止中で取得できず)。**残**: install 用 `.app` の `.icns` + `CFBundleIconFile`(Finder 表示、`V09` の packaging で)、Clair Mobile の AppIcon(旧 iOS 資産は repo に無い)。 |
| `V12` | `done` | `D4` | `V04`, `U04` | **P1**。Project/pane interaction の正しさと応答性を修正。**dogfood follow-up 完了(2026-09-22)**: `project.switch` は Project identity と cache 済み tree/layout を同期的に一括更新し、directory scan / Git status / workspace restore / IPC probe / workspace persistence / explorer row生成を main actor 外へ移した。初回open/restoreも空またはcache状態を即時表示してbackground scanで補完し、Project世代+root guardで古い走査結果を拒否する。file tree は各階層で folder先・安定名前順。Git contextもactive root一致時のみ反映。2,000回の rapid Project switch + pane focus regressionは0.150秒で、tree/tab/focusの混線なし。`WorkbenchProjectTests` / `PaneTreeTests` 21件、全体build、native snapshot通過。**titlebar follow-up(2026-09-23)**: SwiftUI titlebar が AppKit の titlebar を覆っていたため空き領域の double-click が無反応だった。`TitlebarArea`(tab strip の空き・traffic light 横・settings header)で drag=`performDrag`、double-click=`AppleActionOnDoubleClick`(既定 zoom / Minimize / None)を再現。traffic lights を 5pt 右へ(AppKit 既定 x を初回記録し絶対値で再配置)、spacer も 96→101。`ClairMacApp` build 通過、実機のクリック確認は未実施。 |
| `H11` | `done` | `D4` | `H05`, `U04` | **P2**。AI provider の残り使用量を status footer に戻す。旧 Clair にあった provider quota/remaining 表示を、screen scraping ではなく provider の公式 API/hook または根拠のある usage event から取得する。provider ごとの window、使用量/残量、reset 時刻、取得時刻を正規化し、利用不可・未対応・認証切れ・stale を明示する。取得は background で rate-limit/cancel 可能にし、Project/terminal 切替を止めない。OpenCode/Codex/Claude Code のうち取得可能な provider を実データで検証し、取得不能な provider は偽の数値を出さない。 **進捗(2026-09-23)**: Codex を公式 `codex app-server` の `account/rateLimits/read`(stdio JSON-RPC、login shell 経由、12 s timeout)で取得し、`showQuota` ON の間だけ main actor 外で 5 分毎に更新(OFF で `.task` が cancel)。status bar は mock `QuotaMeter` どおり最も逼迫した窓を表示(実データで `Codex 5時間 残り15%`、offscreen 描画で確認)、tooltip に全 provider の window/残量/reset/取得時刻と stale(10 分超)・取得不可(未導入/認証エラー文言)・未対応を列挙。`ProviderQuotaTests` 4 件(live は `CLAIR_LIVE_QUOTA=1` で実 Codex から 5h 85%/7d 76% 取得を確認)、統合後 `ClairCoreTests` 220 件通過。**完了(2026-09-23)**: owner 判断で残り 2 provider を接続。(1) Claude Code は Orca/CodexBar と同じ非公開 `GET api.anthropic.com/api/oauth/usage`(`anthropic-beta: oauth-2025-04-20`、Claude Code UA)を、Claude Code が login Keychain `Claude Code-credentials` に置く OAuth token(`/usr/bin/security` 経由で許可ダイアログなし、`claudeAiOauth.accessToken` 以外は破棄、fallback `~/.claude/.credentials.json`)で呼び、`five_hour`/`seven_day` を表示。期限切れ/401 は「取得できません」、429 は前回値を保持(自然に stale 表示)し `Retry-After`(既定 15 分、最短 5 分)まで再要求しない。agent の起動は無改変のまま(ADR-0002 に触れない)。(2) OpenCode は OpenCode 自身の `auth.json` の `opencode-go` key で `GET opencode.ai/zen/go/v1/usage` を呼び rolling 5h/weekly/monthly を表示(401/未ログインは明示)。`ProviderQuotaTests` 9 件(parser 3 provider・throttle 保持、live は `CLAIR_LIVE_QUOTA=1` で Claude 5h 57%/7d 14%、Codex 5h 38%/7d 98%、OpenCode monthly 76% を実取得)、`make test` PASS。既知の上限: 非公開 endpoint の仕様変更で「取得できません」に落ちる、モデル別週間枠は未表示。実 `.app` 目視は画面ロック中のため `U07` の final QA で行う。 |
| `V13` | `blocked` | `D5` | `V01`, `V04`, `U04` | **P2**。左 navigation の Debug を実機能へ接続する(仕様 §13)。DAP を共通 transport/model とし、Go/Delve を最初の第一級 debugger にする。launch/attach、configuration、breakpoint、continue/pause/step、stack/threads、variables、console、終了/crash を typed command と sidebar/pane UI に配線し、Project/worktree identity を保持する。未設定・Delve 未導入・接続失敗を明示し、実装完了までは Debug navigation を disabled/準備中表示にして無反応にしない。D5 独立レビュー必須。進捗: DAP transport、Go/Delve launch/attach、typed command、Debug sidebar/pane、breakpoint・stack・thread・variables・console、終了回収を統合。D5 独立コードレビュー承認、fake DAP 8 件、make test 228 件成功。残件: この Mac の debugserver が launch 中に停止するため実 Delve の launch/attach・停止・変数・出力を未確認。Mac がロック中で画面 QA も未実施。VS Code 型の起動設定 UI はユーザー方針で native に追加したが Workbench/Design canvas との同期は未実施。 |
| `N10` | `done` | `D4` | `E08`, `T05`, `N04`, `N12`, `N13` | **P2**。mobile app に editor / terminal surface を配線する。`E08`(iOS editor)と `T05`(iOS terminal)の core を、実際に接続・認証済みの client composition 上で editor / terminal 画面として出す。仕様 §10 の terminal attach と入力、ファイルの段階的読み込みを実画面として提供する。mobile に full source editor を移植することは必須にしない。**開始(2026-09-23)**: N12/N13 の共有 TLS-pinned composition と E08/T05 surface core が揃った `rewrite/clair-v2` (`a3249cf`) 上で着手。既存 mock の mobile project/file/terminal 経路を確認し、認証済み session・選択中 scope・画面 lifecycle に接続する。 **Blocked (2026-09-23, design gate)**: `prototypes/clair-workbench/src/mobile-artboards.ts` に `MobileTerminal` artboard はあるが、mobile の file browser・段階的 file read・editor screen が無く、`Main` は 1440×900 の desktop screen。`clair-mock-implement` と task skill の UI gate に従い、canvas に無い native screen は作らない。N12 adapter にも remote paged file-read boundary がまだ無い。Design canvas に mobile file/editor/loading flow を定義し、Workbench と interaction-lab へ同期した後に再開する。N10 の native code は未変更。 **再スコープ(2026-09-23, オーナー決定)**: mobile に editor 機能は不要。N10 は terminal surface の配線のみとし、editor/file browser の design gate は解消。`MobileTerminal` artboard に従い、認証済み composition 上で terminal session 一覧→attach→入力の実画面を出す。host から attach 可能な session と process generation を得る wire call が無いので追加する。 **完了(2026-09-24)**: wire に `terminalSessions` を追加(`ClairRemoteTerminalSession` = scope + process generation)。host は `ClairTerminalBoundary.attachableSessions` で live かつ device scope で read 可能な session だけを返す。adapter/composition 経由で mobile root に「Terminals」一覧を出し、`MobileTerminal` artboard に沿った push 画面(project·worktree 行、`ClairTerminalView`、「表示幅は…」注記、esc/tab/^C/↑/↓ key bar、composer)で attach・入力する。viewport は PTY を resize しない。検証: `ClairRemoteListenerTests` 3 件(一覧→同じ generation で attach、terminal scope の無い device には空一覧。scope filter を外す mutation で fail)、`make test` 233 件、`make build-mobile-simulator` 成功。既知の上限: artboard の欠落 banner・byte 数・epoch 表示は未実装、session 名は sessionID。実機/simulator で host と繋いだ目視は `U07`/`T07` で行う。 |
| `N11` | `done` | `D5` | `H03`, `T04`, `N02` | **P1**。遠隔 client 用の host network transport。承認済み ADR-0016 に従い、host signing key と device grant を永続化し、loopback TLS 1.3 listener、既存 pairing/authorization/terminal boundary を結ぶ request/response wire protocol、resource limit と revoke 時の live channel close を実装する。mobile adapter と app composition は `N12`/`N13` に分離する。D5 独立レビュー必須。 **進捗(2026-09-23)**: codex の lease(worktree 消失)をユーザー指示で解放し `claude/n11-remote-transport`(lease `080265c1`)で引継ぎ。slice 1(`0bde191` host key と grant digest の永続化)を持ち込み、`8eb84a8` で loopback TLS 1.3 listener と wire protocol を実装: 永続 host key の自己署名証明書(`/usr/bin/openssl` → PKCS#12 → メモリ限定 import、keychain 不使用、macOS 15+)、client は host fingerprint に pin、pairing/challenge 認証/terminal attach・long-poll read・ack・detach・input を request/response frame 化(terminal bytes は raw frame)、未認証 10 s 期限・同時数/失敗回数/in-flight/attachment 上限、oversize/不正 frame で切断、revoke で live channel を即切断、pair/revoke 毎に grant を保存。daemon は `--remote-port`(GUI は Stable 47611 / Dev 47612)で 127.0.0.1 のみ bind。検証: `ClairRemoteListenerTests` 8 件(3 回連続 pass、pin/revoke 切断/失敗上限の mutation で fail)、`make test` 225 件、`make mobile-build`、実 daemon で lsof が 127.0.0.1 のみ・SIGTERM で終了。**残**: D5 独立レビュー、統合。ADR §4 の「host key は Keychain」に対し slice 1 は 0600 file 保存(要判断)。agent/workspace/push frame は `N12` で追加。 **完了(2026-09-23)**: D5 独立レビュー(opus)で BLOCKING 2 件 → `ba911ec` で修正 → 再レビュー approved。(1) remote 入力は読み取りループ内で wire 順に逐次 commit し in-flight/busy から除外(全 slot を long-poll で埋めた状態の 20 件パイプライン入力が順序どおり、旧方式への mutation で fail)。(2) grant の snapshot+保存を直列化し、revoke の保存失敗は呼び出し元へ返す。同 session への別 subscriber の attach が既存 attachment を消すバグも修正。統合後 `ClairTransport`/`ClairLocalTerminalHost`/`ClairRemoteListener` 44 件、`make test`、`ClairApps` build PASS。`swift test --filter ClairDaemon` の一括実行はテスト開始前に止まるが、N11 前(`cfa9d48`)でも再現する既存問題(個別実行は全 pass)。**要判断(owner)**: ADR-0016 §4 は host key を Keychain としたが実装は 0700 dir 内の 0600 file(レビューは non-blocking、ADR 追記か follow-up で記録すること)。**follow-up**: host 側 revoke の入口を push registry と一本化(`listener.revoke` は push token を消さない)、未認証 16 slot は tailnet passthrough 越しだと IP 制限できず枯渇し得る、revoke と authenticate の競合で認証直後の channel が次 request まで残る(権限は無い)、agent/workspace/push frame は `N12`。 |
| `N12` | `done` | `D4` | `N11` | **P1**。ADR-0016 に従い、mobile 側の TLS-pinned Network.framework adapter を実装する。`ClairMobileTerminalTransport`、agent、workspace、verifier、push の各 boundary を既存の wire protocol に載せ、`ClairAuthenticatedConnection` の client 用不活性 initializer を追加する。`ClairDaemonKit` を mobile module へ link しない。 **完了(2026-09-23)**: `ClairRemoteMobileAdapter` を追加し、host key と任意の証明書 pin を検証する Network.framework/TLS channel 上で、pair/auth、terminal attach/read/ack/input/detach、agent dispatch、workspace changed-file/diff、session verifier、push register/unregister を実配線。replay 可能な cached cursor は保持し、resync が必要な場合のみ host cursor を採用する。client 用 `ClairAuthenticatedConnection(clientInfo:)` は host authority が知らない不活性 handle。検証: `ClairRemoteListenerTests` 11/11 PASS、`make mobile-build` PASS、`git diff --check` PASS。`make check` は既存 `check_sources` が正規の `ClairMobileKit` 名を v1 dependency として検出するため失敗(現行 iOS build は成功、`ClairDaemonKit` は mobile target に含まれない)。 |
| `N13` | `done` | `D3` | `N12` | **P1**。`ClairMobileClient` をアプリ composition root へ接続し、接続 generation と認証済み connection を conversation、diff、terminal surface へ供給する。reconnect / stale generation / endpoint pin failure を UI state として表示する。**完了(2026-09-23)**: `ClairMobileConnectionComposition` が Keychain identity、`ClairMobileClient`、N12 の共有 TLS-pinned adapter、conversation/diff/reconnect/terminal transport を所有。既存 identity の自動再接続、pairing code と表示 fingerprint の照合、ユーザー確認後の pair、APNs delegate から同じ composition への token/push 転送を配線。SwiftUI には秘密情報を含まない接続 state・host/endpoint 要約・generation を表示し、TLS/host pin 失敗と stale generation を明示する。conversation/diff controller は新しい authenticated session に再bindし、旧 connection の dispatch/read と遅延レスポンスを reject。terminal session は daemon process generation と別に client connection generation を受け取る。検証: N13 generation/pin/stale tests 3 件、terminal generation test 1 件、diff review と conversation dedup regression 19 件、`make mobile-build`、`swift format lint --strict`、`git diff --check` PASS。実機 pairing/TLS smoke は N08 の dogfood gate に残す。 |
| `U03` | `queued` | `D3` | `U02`, `N10` | **P2**。mobile の host/project/session/activity/review/notification navigation を mock contract に合わせる。compact/regular size class と safe area に適応しつつ情報階層を変えない。 |
| `U07` | `queued` | `D4` | `U05`, `U06`, `E11`, `E14`, `V12`, `H11`, `V13` | **P2**。screenshot/interaction regression、VoiceOver、Dynamic Type、keyboard-only、reduced motion、contrast の final QA。2026-09-22 dogfood review の titlebar/tab/sidebar/editor/terminal/search/Git/quota/debugger 修正を実アプリで再確認し、差分は canvas 変更か native bug のどちらかへ分類する。Mac 分を先に行い、iOS 分は `N10`/`U03` が揃ってから追い QA とする。 **U06 引継ぎ(2026-09-23)**: owner 判断で U06 を先に完了扱いとした分の、複数 terminal の caret/dim 実画面確認と GPU frame time 計測を Mac QA で確認する。 |
| `T07` | `blocked` | `D5` | `T04`, `T06`, `T09` | **P3**。terminal integration gate。OpenCode TUI、shell、resize、alternate screen、flood、sleep/wake、network switch、Mac/mobile 同時入力、reattach、OSC 52/633 の実機 test と resource limits。`T09` の後でなければ「同じ session への同時入力」を実アプリで検証できない。進捗(2026-09-23): 実 OpenCode binary の `--version` を含む PTY/terminal 31 件、H10 resource limit 18 件、統合先の vendored libghostty surface test 11 件(3 skip) が成功。H10 runtime stop test の固定一時ディレクトリ衝突を UUID path に修正し再実行成功。残件: Mac がロック中で実 GUI の OpenCode TUI、alternate screen、OSC 52/633、sleep/wake、network switch、flood を目視・操作できない。Mac/mobile 両実アプリからの同時入力は mobile composition/transport (`N10`/`N12`/`N13`) 未完了。D5 完了レビューは実機確認後に行う。 |
| `N08` | `blocked` | `D5` | `H10`, `N06`, `N07`, `T02`, `N09`, `N13` | **P3**。Mobile-on-Clair dogfood gate。iPhone だけを操作して実 terminal session 上で agent を起動し、依頼・承認・diff 確認・follow-up・完了通知・再接続まで行う。**Blocked**: iPhone/iPad 実機、Apple signing、APNs の外部依存。`N09` で Mac 側ペアリング bootstrap 面は解消済み。 |
| `V10` | `queued` | `D5` | `V02`, `V03`, `V04`, `V05`, `V06`, `V07`, `V08`, `V09`, `T09`, `E11`, `U04`, `U05`, `U06`, `V12`, `V14`, `V15`, `H11` | **P3**。Clair-on-Clair cutover gate。Stable から Clair source を開き、terminal/agent で Dev を build・起動して変更を確認できる。2026-09-22 dogfood review の daily-driver blocker が解消され、ccedit より快適と本人が確認する。CLI/MCP 経由の scripted 操作でも同じ流れが通ること。Debugger (`V13`) は仕様 §13 の後続ロードマップであり cutover 条件には含めないが、未実装中の navigation は `U04` で無反応に見せない。 |

## 完了タスク

証跡の詳細は Git 履歴と各タスクの設計 doc にある。

| task | status | difficulty | depends | outcome |
|---|---|---|---|---|
| `B00` | `done` | `D2` | — | pre-rewrite checkpoint commit と archive tag。 |
| `B01` | `done` | `D3` | `B00` | Swift packages、macOS app、iOS app、daemon executable の target と build/test lane。 |
| `B02` | `done` | `D3` | `B00` | native mobile + APNs 方針の ADR(0015)。PWA 優先の旧方針を superseded に接続。 |
| `B03` | `done` | `D5` | `B01` | `ProjectID`/`WorktreeID`/`SessionID`、revision、operation ID、capability、error、event envelope の共有型。 |
| `H01` | `done` | `D4` | `B03` | GUI から独立した `ClairDaemon` lifecycle、single-instance ownership、local control channel。 |
| `H02` | `done` | `D3` | `H01` | project/worktree catalog、file tree、bounded file read、changed-file summary の read-only API。 |
| `H03` | `done` | `D5` | `B02`, `B03`, `H01` | client transport、one-time pairing、device key、host fingerprint、grant scope、revoke。threat tests 通過。 |
| `H04` | `done` | `D5` | `H02` | OpenCode provider adapter。起動・再開・停止を project/worktree/session identity に関連付け。 |
| `H05` | `done` | `D5` | `H04` | provider 非依存な conversation/tool-call/attention/completion/usage イベント正規化。 |
| `H06` | `done` | `D5` | `H03`, `H05` | prompt/approval/deny/interrupt/stop を scoped command として実装。 |
| `H07` | `done` | `D4` | `H02` | Git status、changed-file list、text/binary diff、hunk metadata。 |
| `H08` | `done` | `D5` | `H03`, `H05`, `H06` | session journal、subscriber cursor、gap/resync、revision snapshot。 |
| `H09` | `done` | `D4` | `B02`, `H03` | 最小 `ClairPushRelay` と APNs provider boundary。opaque event payload。 |
| `H10` | `done` | `D5` | `H06`, `H07`, `H08`, `H09` | daemon の crash recovery、resource limits、structured diagnostics。 |
| `N01` | `done` | `D3` | `B01`, `B02` | SwiftUI native app、composition root、environment、shared package 境界。 |
| `N02` | `done` | `D4` | `B03`, `H03`, `N01` | typed client、Keychain device identity、pairing handshake、certificate 検証。 |
| `N03` | `done` | `D3` | `N02` | host list、pair/re-pair、connection state、device scope、revoke。 |
| `N04` | `done` | `D3` | `H02`, `N03` | project/worktree/session browser と recent destination。 |
| `N05` | `done` | `D4` | `H05`, `H06`, `N04` | conversation stream、prompt composer、attention、approval。 |
| `N06` | `done` | `D4` | `H07`, `N05` | changed-file list、native diff、hunk navigation、review follow-up。 |
| `N07` | `done` | `D4` | `H08`, `H09`, `N03` | APNs registration、notification category、deep link、scene lifecycle。 |
| `N09` | `done` | `D4` | `H03`, `N03` | Mac 側ペアリング bootstrap 面(`N08` が発見した gap の解消)。 |
| `E01` | `done` | `D4` | `B01` | editor invariants、Unicode corpus、10MB/long-line fixture、benchmark harness、回帰の下限。 |
| `E02` | `done` | `D5` | `E01` | rope text storage、line index、stable line ID、座標変換、immutable snapshot。 |
| `E03` | `done` | `D5` | `E02` | transaction、SelectionSet、multi-cursor、矩形選択、Undo/Redo、外部編集マージ。 |
| `E04` | `done` | `D3` | `E03` | literal/regex search、replace preview、replace one/all、選択範囲への適用。 |
| `E05` | `done` | `D4` | `E02` | Tree-sitter incremental parse と LSP coordinate/lifecycle。 |
| `E06` | `done` | `D5` | `E02`, `E05` | macOS custom `NSView`、CoreText visible-line layout、scroll、hit test、caret、selection。 |
| `E07` | `done` | `D5` | `E03`, `E06` | `NSTextInputClient`、日本語 IME、marked text、clipboard、drag/drop、accessibility。 |
| `E08` | `done` | `D5` | `E03`, `E05` | iOS/iPadOS editor surface、touch selection、hardware keyboard、IME、viewport virtualization。 |
| `E09` | `done` | `D5` | `E03`, `E05`, `N06` | revision-aware review anchor、line comment、thread、stale/orphaned、suggestion transaction。 |
| `E10` | `done` | `D5` | `E04`, `E07`, `E08`, `E09` | editor integration gate。edit/multi-cursor/search/review/save/外部編集と性能閾値。 |
| `T01` | `done` | `D5` | `B01` | libghostty の pinned build、license、resource bundle、ABI pin。 |
| `T02` | `done` | `D5` | `H01`, `H04`, `H05`, `H06`, `H08` | daemon-owned PTY/process/session ownership と raw I/O bridge。 |
| `T03` | `done` | `D4` | `T01`, `T02`, `T08` | macOS Ghostty surface を workspace に接続。selection、copy/paste、scrollback、font/DPI、resize。 |
| `T04` | `done` | `D5` | `T02`, `H08` | remote terminal binary stream、epoch/cursor、snapshot/gap、backpressure。 |
| `T05` | `done` | `D5` | `T01`, `T04`, `N01`, `T08` | iOS/iPadOS Ghostty surface と remote session attach。 |
| `T06` | `done` | `D4` | `T03`, `T05` | IME/CJK、paste guard、mouse reporting、focus、desktop-owned PTY geometry。 |
| `T08` | `done` | `D5` | `T01` | Ghostty surface ABI pin(`T03` が発見した gap の解消)。 |
| `U01` | `done` | `D2` | `B01` | Design canvas と Workbench の同期。対象 screen/state、tokens、layout の freeze。 |
| `U02` | `done` | `D3` | `U01` | canvas tokens を Swift の color/type/spacing/radius/motion primitive へ。 |
| `V01` | `done` | `D5` | `B03` | typed Command Registry(22 command)。palette/menu/shortcut を projection 化。 |
| `V02` | `done` | `D4` | `V01`, `H01` | user-scoped Unix socket IPC(0600/0700、peer uid 検証)と `clair` CLI 基盤。 |
| `V04` | `done` | `D4` | `V01`, `H02` | Project model と workspace 永続化。Git の有無を問わない folder、実 file tree、layout 復元。 |
| `V07` | `done` | `D4` | `V01`, `V04`, `T03` | agent launch profile を raw terminal で複数起動。worktree cwd 対応。 |
| `V08` | `done` | `D3` | `V04`, `V07` | Project badge、通知 history、macOS banner、Project/terminal 単位 mute。事実 signal のみ。 **追補(2026-09-24)**: Claude Code/Codex は Ghostty 上で BEL ではなく OSC 9/777 で通知を要求するため banner が出ていなかった。`GHOSTTY_ACTION_DESKTOP_NOTIFICATION` を bell と同じ事実として数える(title/body は agent 由来で code を含み得るので decode しない — mobile push payload にも text の経路が無いまま、`INV` の「通知に機密を載せない」を維持)。表示文言は「ベル」→「通知」。実 surface で OSC 9 と 777 が各 1 件数えられる統合 test を追加し、C 側を外す mutation で fail を確認。**残**: 実アプリで Claude Code 実行中の banner 目視、mobile 転送は `N08` の APNs 経路で `WorkbenchNotice.kind` だけを送る。 |

## 統計

- 完了 70 / 全 77(2026-09-24 時点)
- 残り 7(queued 3、blocked 4)
- `python3 .agents/skills/clair-task/scripts/task_lease.py validate` がこの数を検証する。
